import styled from 'styled-components'
import { SectionWrapper } from '../../../components/Containers'

export const StyledWrapper = styled(SectionWrapper)`
  border-top: 10px solid ${(props) => props.theme.colors.sage};
`
